conda create -n 2026speech python=3.10 -y
conda activate 2026speech

# Core scientific stack
conda install -c conda-forge pandas matplotlib numpy scikit-learn tqdm -y

# Audio + feature extraction
conda install -c conda-forge librosa soundfile opensmile -y

# PyTorch interpretability
pip install captum
